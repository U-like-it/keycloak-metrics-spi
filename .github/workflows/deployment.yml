name: keycloak-spi-pipeline
on:
  push:
    branches:
      - production
jobs:
  release-keycloak:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Read version.yaml
        id: version
        uses: juliangruber/read-file-action@v1
        with:
          path: ./version.yaml

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: helm repo add keycloak
        uses: koslib/helm-eks-action@master
        env:
          KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
        with:
          command: helm repo add codecentric https://codecentric.github.io/helm-charts
      - name: helm update keycloak
        uses: koslib/helm-eks-action@master
        env:
          KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
        with:
          command: helm repo update
      - name: helm deploy keycloak
        uses: koslib/helm-eks-action@master
        env:
          KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
        with:
          command: helm upgrade keycloak --install codecentric/keycloak --values ./keycloak.yaml

      - run: echo "🎉 The keycloak deployment is finished."
